<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :rating %>
	<br><%= radio_button_tag ':rating', 'Amazing' %> Amazing
    	<br><%= radio_button_tag ':rating', 'Good' %> Good
    	<br><%= radio_button_tag ':rating', 'Meh' %> Meh
    	<br><%= radio_button_tag ':rating', 'Horrible' %> Horrible
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
